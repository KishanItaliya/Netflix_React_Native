import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  image: {
    width: 110,
    height: 180,
    resizeMode: "cover",
    borderRadius: 5,
    margin: 5,
  },
  title: {
    fontSize: 18,
    fontWeight: "bold",
  },
});

export default styles;
